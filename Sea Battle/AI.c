/*
#include <stdlib.h>
#include <time.h>
#include "AI.h"
#include "INFO.h"
#include "MAKE_SHIP.h"
#include "PLAYER_ACT.h"

void AI(void){
	int x, y;

	while (1){
		srand((unsigned)time(NULL));
		x = rand() % 10;
		y = rand() % 10;

		if (PLAYER1_MAP[y][x] != EMPTY_SHOW && PLAYER1_MAP[y][x] != DESTROY_BLOCK){
			PLAYER1_MAP[y][x] = DESTROY_BLOCK;

			\
			
		}
		
	}
}
*/